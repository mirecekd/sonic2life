# ── AWS Credentials ──────────────────────────────────────
# For Docker deployment (no profile support)
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key
# AWS_SESSION_TOKEN=  (only if using temporary credentials)
AWS_REGION=eu-north-1

# ── Amazon Nova 2 Sonic (Voice AI) ───────────────────────
NOVA_SONIC_MODEL_ID=amazon.nova-2-sonic-v1:0
NOVA_SONIC_VOICE_ID=matthew

# ── Weather (optional) ───────────────────────────────────
# OpenWeatherMap API key – weather tool disabled if empty

# ── Push Notifications (optional) ────────────────────────
# VAPID keys for Web Push – auto-generated if empty
# Must be raw urlsafe base64 (~43 chars private, ~87 chars public)
VAPID_PRIVATE_KEY=
VAPID_PUBLIC_KEY=

# ── Server ───────────────────────────────────────────────
HOST=0.0.0.0
PORT=5005
# Weather uses Open-Meteo API (free, no API key needed)

# ── Authentication (app-level, PWA-compatible) ──────────────
# Set both to enable login page. Leave empty for open access.
AUTH_USERNAME=
AUTH_PASSWORD=
